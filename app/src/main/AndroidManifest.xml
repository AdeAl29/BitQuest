<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Izin Notifikasi (Tetap Ada) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- BARU: Izin Internet (Wajib untuk Firebase) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HabitTracker"
        tools:targetApi="31">

        <!-- AuthActivity SEKARANG menjadi titik masuk aplikasi -->
        <activity
            android:name=".AuthActivity"
            android:exported="true"
            android:theme="@style/Theme.HabitTracker"> <!-- Atau NoActionBar jika diinginkan -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity sekarang BUKAN lagi titik masuk -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.HabitTracker"
            android:windowSoftInputMode="adjustResize" />

        <!-- Splash Activity kita non-aktifkan sementara -->
        <activity
            android:name=".SplashActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.NoActionBar"/>

    </application>

</manifest>

